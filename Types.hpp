#pragma once

namespace Tokens {   
  enum Types {VAR, COS, SIN, TG, CTG, EXP, LN, NUM, 
	      PLUS, MINUS, MULT, DIV, UPPER, LBR, RBR, 
	      FUNC, OP};
}

namespace Syntax {
  enum Types {UNO, BIN, NUM, VAR};
}
